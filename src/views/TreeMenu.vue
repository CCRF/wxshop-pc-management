<template>
  <div>
    <li>
      <router-link :to="menu.url" v-if="menu.url!=null"><i class="fa fa-home"></i>{{ menu.name }}</router-link>
      <a v-else><i class="fa fa-home"></i>{{ menu.name }}</a>

      <ul class="submenu" v-if="menu.children.length>0">
        <tree-menu v-for="childMenu in menu.children" :menu='childMenu' :key="childMenu.id"></tree-menu>
      </ul>
    </li>
  </div>
</template>

<script>
export default {
  name: "TreeMenu",
  props: ['menu']
}
</script>

<style scoped>
    li {
        list-style: none;
        position: relative;
        border-style: solid;
        border-width: 1px 0 0;
        border-color: #E5E5E5;
    }

    .submenu > li {
        position: relative;
        padding-left: 40px;
    }

    a {
        display: block;
        width: 189px;
        height: 36px;
        color: #5d5d5d;
        text-decoration: none;
        line-height: 36px;
    }

    .fa {
        display: inline-block;
        margin-left: 14px;
        margin-right: 14px;
        font-family: 'FontAwesome';
        font-weight: normal;
        font-style: normal;
    }

    .submenuHide {
        display: none;
        position: relative;
    }

    .submenuShow {
        display: block;
        position: relative;
    }


    .submenu:before {
        content: "";
        display: block;
        position: absolute;
        z-index: 1;
        left: 18px;
        top: 0;
        bottom: 0;
        border: 1px dotted;
        border-width: 0 0 0 1px; /* 上 右 下 左 */
    }

    .submenu > li:before {
        content: "";
        display: block;
        width: 10px;
        position: absolute;
        z-index: 1;
        left: 20px;
        top: 17px;
        border: 1px dotted;
        border-width: 1px 0 0; /* 上 右 下*/
    }
</style>